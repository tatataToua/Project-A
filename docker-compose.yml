services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: askme
      POSTGRES_PASSWORD: askme
      POSTGRES_DB: askme
    ports:
      - "5432:5432"
    volumes:
      - askme_pgdata:/var/lib/postgresql/data

volumes:
  askme_pgdata:
